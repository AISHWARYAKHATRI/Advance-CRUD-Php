Php CRUD
Its a project with CRUD, Multiple image upload, Search Functionality, Pagination.
This makes use of 2 database tables. One for storing the user details and the other for storing the multiple images of each user.
Flow:
Registration Page(with image preview) -> Response Page -> Display Page(update, delete, profile-view, multiple delete, all-delete, search, pagination options)


